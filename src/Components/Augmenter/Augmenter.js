import React from 'react';
import classes from './Augmenter.module.css';

const Augmenter = (props) => {
  return <button className={classes.addBtn}>+ Add To Cart</button>;
};

export default Augmenter;
